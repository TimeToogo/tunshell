name: Testing

on:
  push:
    branches:
      - test

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - run: curl -sSf https://lets.tunshell.com/init.sh | sh /dev/stdin T PHSb2urreZKipshOcRo955 VmdbZ8DBSiJ7WbBfhzj50x
     
