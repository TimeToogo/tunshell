name: Testing

on:
  push:
    branches:
      - test

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - run: sh <(curl -sSf https://lets.tunshell.com/c6c95738-4398-4e70-8034-f5c82fca6fec.sh)
     